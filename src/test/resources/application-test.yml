spring:
  config:
    activate:
      on-profile: test

  main:
    lazy-initialization: true

  flyway:
    enabled: false

  kafka:
    listener:
      auto-startup: false

  task:
    scheduling:
      enabled: false

app:
  mail:
    enabled: false
  outbox:
    relay-enabled: false
  storage:
    enabled: false